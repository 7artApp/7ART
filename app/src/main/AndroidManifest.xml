<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.br.seventh_art">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.7ART">

        <activity android:name=".view.recommendations.movies.activity.MoviesRecommendationsActivity" />
        <activity android:name=".view.MovieReadMoreActivity" />
        <activity android:name=".view.login.activities.SignInActivity" />
        <activity android:name=".view.login.activities.SignUpActivity" />
        <activity android:name=".view.login.activities.LoginActivity" />

        <activity android:name=".view.splashscreen.SplashActivity" />
        <activity android:name=".view.profile.ProfileActivity" />
        <activity android:name=".view.genres.series.activity.SeriesGenresActivity" />
        <activity android:name=".view.genres.movies.activity.MoviesGenresActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>